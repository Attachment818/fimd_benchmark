# Paper figure exports

This directory contains only manually selected, lightweight figure candidates for
the paper. Full method outputs, raw aligned-image collections, checkpoints, and
datasets remain excluded from Git.

Before committing a server result, copy only the selected overlay into a method or
pair subdirectory and include the corresponding `pair_result.json` for traceability.
